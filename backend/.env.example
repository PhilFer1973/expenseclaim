# ── Supabase ──────────────────────────────────────────────────────────────────
# Find these in: Supabase dashboard → Project Settings → API
SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGc...   # service_role key (NOT anon key — never ships to device)

# ── Storage ───────────────────────────────────────────────────────────────────
# Leave as "receipts" unless you created the bucket with a different name
RECEIPTS_BUCKET=receipts

# ── AI / LLM ──────────────────────────────────────────────────────────────────
# Emergent platform key — used by ai_service.py for Claude Vision, category
# suggestions, and narrative summarisation (receipt extraction + AI features)
EMERGENT_LLM_KEY=sk-emergent-...

# OpenAI key — used by embedding_service.py for text-embedding-3-small (1536-d)
# Required for category suggestion kNN and duplicate detection
OPENAI_API_KEY=sk-...
